# Keep empty in local development so Vite proxies /api and /ws to localhost:3001.
# Set this to the public HTTPS API origin for a production build.
VITE_API_ORIGIN=

# Keep this in sync with server ACCOUNT_FIXED_CODE for local fixed-code mode.
VITE_FIXED_VERIFICATION_CODE=252616
